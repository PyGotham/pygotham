server {
    # TODO: www?
    server_name pygotham.org;
    #return 301 https://$host$request_uri;

    # TODO: enable, add HSTS
    #ssl on;
    #ssl_certificate /etc/keys/ssl-unified-2.crt;
    #ssl_certificate_key /etc/keys/server.key;

    #ssl_session_timeout 5m;

    #ssl_protocols       SSLv3 TLSv1 TLSv1.1 TLSv1.2;
    #ssl_ciphers         HIGH:!aNULL:!MD5;

    location /static/ {
        alias /home/pygotham/pygotham/pygotham/frontend/static/;
    }

    location /media/ {
        alias /home/pygotham/pygotham/pygotham/frontend/media/;
    }

    location / {
        uwsgi_pass 127.0.0.1:8000;
        include uwsgi_params;
    }
}
